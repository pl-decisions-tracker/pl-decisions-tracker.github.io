---
type Props = {
  searchClass: string;
};

const { searchClass } = Astro.props;
---

<>
  <input
    class="form-control data-filter"
    type="search"
    name={`${searchClass}`}
    id={`${searchClass}`}
    data-filtertype="search"
    data-filtervalue=""
  />
</>
